export function toUppercase(str: string) {
  return str.toUpperCase()
}

export function getPerson() {
  return { name: 'Niraj' }
}

export function getPersonList() {
  return [{ name: 'Niraj' }, { name: 'Suraj' }]
}
